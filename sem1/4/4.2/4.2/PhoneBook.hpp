#pragma once

#include "list.hpp"

int findPhone(List *list, char name[]);
void findName(List *list, long long number, char *word);
void saveFile(List *list, char fileName[]);
void readFile(List *list, char fileName[]);
